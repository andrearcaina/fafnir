mutation CreatePortfolioAccount($request: CreateAccountRequest!) {
  createAccount(request: $request) {
    code
    data {
      id
      accountNumber
      type
      currency
      balance
    }
  }
}
